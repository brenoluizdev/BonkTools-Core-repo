BONK_USERNAME=
BONK_PASSWORD=

# development | production
NODE_ENV=development

# Blob LZ-String do estado inicial do jogo (capturado via: pnpm tsx src/capture-is.ts <URL>).
# Se não definido, o bot usa startCountdown(1) como fallback.
# BONK_INITIAL_STATE=
